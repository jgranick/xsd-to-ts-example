{
  "name": "xsd-to-ts-example",
  "version": "1.0.0",
  "description": "A sample project that converts XSD files to TypeScript definitions",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && node dist/index.js",
    "dev": "ts-node src/index.ts",
    "start": "node dist/index.js",
    "clean": "rimraf dist"
  },
  "keywords": ["xsd", "typescript", "conversion", "schema"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "fast-xml-parser": "^4.3.0",
    "xml2js": "^0.6.0"
  }
}
